from fastapi import FastAPI
# This file exists solely to satisfy Vercel's automatic entrypoint detection
# and suppress the build warning. The actual routing is handled by api/[...path].py
app = FastAPI()
